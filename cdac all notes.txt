1)

npm init

npm install express --save 


2)

git add .

git commit -m "sms"

git push


3)

sudo apt update

sudo apt install git

git clone

sudo apt install docker.io

cd/app

sudo docker build . -t apponvm

sudo docker images

sudo systemctl start docker

sudo systemctl status docker

sudo run docker --name runningapp -d -p 5000:5000 apponvm


= = = = = = = = = = = = = = = = = = = 

dockerfile

from     node:7
workdir /app
copy  package.json /app
run  npm install
copy ./app/
cmd  node server.js
expose 5000


= = = = = = = = = = = = = = = = = = = 

server file

const express=require('express')
app = express();

//const path = require('path')
//app.use(express.static(path.join(__dirname, "public")))

app.get('/', (req,res)=>{
res.sendfile("public/login.html", {root:__dirname});
})

app.listen(5000);







